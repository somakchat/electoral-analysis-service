# Political Strategy Maker - AWS Lambda Dependencies
# Minimal set for AWS: OpenSearch + DynamoDB + OpenAI API
# NO local dependencies: FAISS, scipy, scikit-learn, pandas

# Core Framework
fastapi==0.109.0
pydantic==2.6.0
pydantic-settings==2.1.0
python-multipart==0.0.6
mangum==0.17.0

# Document Processing (minimal)
python-docx==1.1.0
openpyxl==3.1.2
pypdf==3.17.0
chardet==5.2.0

# LLM Provider (OpenAI only - for embeddings and chat)
openai==1.12.0
tiktoken==0.6.0

# AWS SDK
boto3==1.34.34
requests-aws4auth==1.2.3
opensearch-py==2.4.2

# Async & WebSocket
websockets==12.0
httpx==0.26.0
aiohttp==3.9.3

# Utilities
requests==2.31.0
python-dotenv==1.0.1
structlog==24.1.0
tenacity==8.2.3

# NOTE: The following are EXCLUDED for AWS Lambda:
# - faiss-cpu (using OpenSearch instead)
# - scipy (not needed)
# - scikit-learn (not needed)
# - pandas (not needed - data already in OpenSearch)
# - numpy (not needed directly)
# - sentence-transformers (using OpenAI API)
# - torch (too large)
