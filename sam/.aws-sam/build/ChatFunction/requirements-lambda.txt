# Political Strategy Maker - Lambda-optimized Dependencies
# Use this for AWS Lambda deployment (smaller size, no torch)

# Core Framework
fastapi==0.115.6
uvicorn[standard]==0.32.1
pydantic==2.10.4
pydantic-settings==2.6.1
python-multipart==0.0.19
mangum==0.17.0  # ASGI adapter for Lambda

# Document Processing
python-docx>=1.2.0
openpyxl==3.1.5
pypdf>=5.9.0
chardet>=5.0.0

# Data Science (CPU-only, minimal)
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0

# Vector Search & RAG (CPU-only)
faiss-cpu==1.9.0.post1
rank-bm25==0.2.2

# LLM Providers
openai>=1.0.0
google-generativeai>=0.8.0

# AWS SDK
boto3>=1.34.0
requests-aws4auth>=1.2.0

# Async & WebSocket
websockets>=12.0
httpx>=0.25.0
aiohttp>=3.9.0

# Utilities
requests>=2.32.0
python-dotenv>=1.0.0
structlog>=24.0.0
tenacity>=8.0.0

# NOTE: The following are EXCLUDED for Lambda:
# - torch (too large, 2GB+)
# - transformers (requires torch)
# - sentence-transformers (requires torch)
# - crewai (not needed, using custom agents)
# - langchain (not needed, using custom RAG)
# - scipy (included in scikit-learn)
# - opensearch-py (only if using OpenSearch)

# For Lambda, embeddings are generated via OpenAI API instead of local models

